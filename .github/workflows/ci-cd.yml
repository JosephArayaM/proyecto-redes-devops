name: Test

on:
  push:
    branches: [main]


jobs:
    test:
        runs-on: ubuntu-lastest

        steps:
            - uses: actions/checkout@v4
            - name: print message
              run: echo "Hello world"